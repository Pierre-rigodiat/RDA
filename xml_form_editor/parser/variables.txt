* List of all $_SESSION variables used in the parser *
------------------------------------------------------

$_SESSION['xsd_parser'] The array containing all the current parser information
TODO Change 'xsd_parser' by 'app_data'

	['conf'] Configuration of the parser
			['dirname'] The directory of the parser within the app
			['modules_dirname'] The directory where all the modules are stored
			['debug'] To use debug or not
			
	['parser'] The parser object (serialized) // TODO Change the name to 'manager'
	
	['mhandler'] The module handler (serialized) // FIXME remove it
	
	['phandler'] The page handler (serialized) // FIXME remove it
	
	['display'] The Display object
	
	['xsd_filename'] name of the schema
	
	['modules'] All the modules
	
TODO list
	['username'] The name of the user (work on it to avoid override of data)
